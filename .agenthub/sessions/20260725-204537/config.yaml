session_id: 20260725-204537
task: "Build a Jewel Stone admin panel at /admin for a Next.js 14 App Router site, WITHOUT breaking the existing site. Password-gate /admin with ADMIN_PASSWORD env + a signed HTTP-only session cookie. Persist all admin data in the existing Upstash Redis (env KV_REST_API_URL / KV_REST_API_TOKEN; reuse the REST pattern in lib/custom-request-store.ts). Features: (1) Dashboard overview tiles. (2) Inventory: list every catalog product (data/products.ts) with editable on-hand stock (default 1) and show/hide-on-website toggle; website shows sold-out at stock 0, and the Stripe webhook (app/api/webhook/route.ts) atomically decrements stock on sale. (3) Add product via dialog + bulk Excel (.xlsx) upload; per-product image upload where the owner picks how many images show on the website, with a missing-images gate before publish. (4) Orders + customers: on every purchase capture customer (name, email, phone, last purchase, date) into a customer base + orders list. (5) Invoices + memos: Jewel Stone-branded templates with sequential numbering; list all issued docs by number/customer/date; emailable via Resend. Simple, easy to use, fully wired, nothing broken. Reuse existing conventions (Resend, lib/stripe.ts + app/api/webhook, CSS Modules, products catalog)."
agent_count: 3
base_branch: main
created: 2026-07-26T00:45:37.345280+00:00
